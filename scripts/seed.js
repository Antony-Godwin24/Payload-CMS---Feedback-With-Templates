// Placeholder file: the working seeder is `scripts/seed.mjs` (ESM).
// This file was previously corrupted during edits and caused a syntax/runtime error.
// Keep a minimal valid export here so importing this file does not throw.

// If you want to run the seeder, run the ESM script instead:
//   pnpm run seed
// which executes: node ./scripts/seed.mjs

export default null
